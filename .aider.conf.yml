# Aider-Konfiguration fuer {{PROJEKTNAME}}.
# AGENTS.md (Projektregeln) und docs/ai/board.md (aktueller Stand) werden bei jeder Session
# automatisch in den Kontext geladen. Details/Ausfuellhinweise: docs/project/setup.md.

read:
  - AGENTS.md
  - docs/ai/board.md

# Aider hat keine Sub-Agenten: nur die Architect/Editor-Trennung (--architect, --editor-model, /code).
# Die Rolle des Orchestrators uebernimmt hier der Mensch, die Schritte laufen nacheinander.
# Siehe AGENTS.md, Abschnitt "Worker starten".
