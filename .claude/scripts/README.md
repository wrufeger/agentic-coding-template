# Scripte für wiederkehrende, token-intensive Vorgänge

Regel (`CLAUDE.md` § 3): Was komplex, langwierig und wiederkehrend ist, wird einmal als Script gebaut und
danach nur noch ausgeführt — der Sub-Agent wertet nur die Ausgabe aus. Stdlib bevorzugt, Secrets nur aus
`.env`/der lokalen Umgebung (nie hart codiert, nie in der Ausgabe), Kopfkommentar mit Zweck/Aufruf/
Ausgabeformat.

**Voraussetzung: Python 3.9+ im `PATH`** (`python3 --version`/`python --version`, siehe `README.md` §
„Voraussetzungen"). Ein `SessionStart`-Hook in `.claude/settings.json` prüft das bei jeder Sitzung und meldet
fehlendes/zu altes Python als einzeilige Warnung, ohne die Sitzung abzubrechen — die Scripte selbst laufen
dann schlicht nicht.

| Script | Zweck | Aufruf | Stand |
| :--- | :--- | :--- | :--- |
| `ai-log.py` | Agenten-Protokoll `ai.log` schreiben/mitlesen (`AGENTS.md` § Logging); Hook-Modus für Claude Code, CLI-Modus für jeden Assistenten mit Shell | `python .claude/scripts/ai-log.py <LEVEL> <agent> <topic> "<Text>"` · `--status` · `--tail [--grep X] [--lines N] [--no-color]` · `--reset` · `--hook` (nur aus `settings.json`) · Env `AI_LOG_RAW=1` (Diagnose, unmaskiert) | 2026-09-11 |
| `update-template.py` | Template-Updates per Git-Merge einspielen (`.claude/template.json`), Platzhalterwerte bleiben erhalten; `--check` läuft zusätzlich automatisch per `SessionStart`-Hook; `--graft` verknüpft nachgerüstete Projekte (kein gemeinsamer Vorfahr) per leerem Merge mit dem Template; `--conflicts` liefert dem Assistenten während eines laufenden Merges je offenem Konflikt Art, Prioritätsregel, Zeilenumfang je Seite und einen Umbenennungs-Kandidaten unter `docs/ai/` (bei „vom Projekt gelöscht, im Template geändert" wie auch bei beidseitiger Umbenennung); **abgewählte Pfade bleiben draußen** — was `AI-CONFIG.md` auf `aus` stehen hat (Wartung, Code-Optimierung, gestrichene KI-Werkzeuge), wird weder als Konflikt vorgelegt noch als neue Datei still eingespielt | `python .claude/scripts/update-template.py --init [--url U] [--base H] [--set K=V ...]` · `--set K=V ...` · `--check [--quiet]` · `--apply [--commit]` · `--continue [--commit]` · `--conflicts` · `--abort` · `--status` · `--graft` | 2026-09-15 |
| `create-project.py` | Weg 1 „Neues Projekt", dünner CLI-Wrapper: `AI-CONFIG.md` einlesen, Platzhalter ersetzen, nicht genutzte Werkzeug-Dateien entfernen, `AI_LOG`/`AI_LOG_LEVEL` setzen, Werte in `.claude/template.json` schreiben (inkl. `applied_config` — Grundlage für `sync-config.py`); `--finish` schreibt `AI-CONFIG.md` fort (Freitext-Abschnitte raus, Datei bleibt) statt sie zu löschen. Die eigentliche Logik steckt in `setup-lib.py`; verschwindet beim Abschluss der Einrichtung (`/act-finalize`), die Bibliothek bleibt | `python .claude/scripts/create-project.py --dry-run` (Default) · `--apply` · `--finish` · `--check` (Selbstpruefung der Pfadlisten, laeuft auch im Plan mit) · `--detect` (welches KI-Werkzeug fuehrt aus) | 2026-09-14 |
| `setup-lib.py` | Fassade + eigentlicher Setup-Ablauf zu Weg 1 „Neues Projekt" (`cmd_dry_run`/`cmd_apply`/`cmd_finish`/`cmd_check`, `main`): AI-CONFIG.md parsen, Platzhalter ersetzen, nicht genutzte Werkzeug-Dateien entfernen, `.claude/template.json` schreiben. Lädt `config-lib.py`/`files-lib.py`/`claudemd-lib.py` per `importlib` und hebt deren Namen in den eigenen Modul-Namensraum (`_merge_module`) — Aufrufer wie `create-project.py`/`sync-config.py` sehen weiterhin ein einziges `setup-lib.py` (`cp.<name>`), war vorher 2461 Zeilen und blockierte parallele Aufträge auf derselben Datei. Bleibt auch nach dem Abschluss der Einrichtung, weil `AI-CONFIG.md` laufend wirkt | kein eigener Aufruf — über `create-project.py` bzw. `sync-config.py` | 2026-09-16 |
| `config-lib.py` | Bibliothek zu `setup-lib.py`: alles rund um `AI-CONFIG.md` — Wortlisten der zulässigen Werte je Schlüssel, Tabellen-/Altformat-Parsing (`parse_config`/`load_config`), Normalisierer je Schlüssel (`normalize_*`), Platzhalterwerte/Orchestrator-Rufname (`compute_values`), Schnappschuss für `sync-config.py` (`build_applied_config`); dazu (Backlog B37) der Live-Abgleich der Tabellen-Schlüssel gegen die Template-Fassung (`fetch_template_ai_config_text`, `missing_ai_config_rows`, `insert_missing_ai_config_rows`, `ai_config_missing_keys`) — lädt `update-template.py` selbst per `importlib` nach, kein Ring. Hängt von keinem der beiden anderen neuen Module ab | kein eigener Aufruf — über `setup-lib.py` | 2026-09-17 |
| `files-lib.py` | Bibliothek zu `setup-lib.py`: Datei-/Pfadoperationen — Platzhalter-Ersetzung, Datenverlust-Schutz beim Entfernen (`ungesicherte_pfade`) samt der drei `remove_*_files`, Hook-Pflege in `.claude/settings.json`, Orchestrator-Modell/Logging-Schalter setzen, JSON-/Text-Schreibhelfer. Braucht `TOOL_FILES` aus `config-lib.py` | kein eigener Aufruf — über `setup-lib.py` | 2026-09-17 |
| `claudemd-lib.py` | Bibliothek zu `setup-lib.py`: Textchirurgie an `CLAUDE.md` bei „Wartung: ein/aus" (`remove_maintenance_references`/`add_maintenance_references`). Braucht die Text-Schreibhelfer aus `files-lib.py` | kein eigener Aufruf — über `setup-lib.py` | 2026-09-16 |
| `sync-config.py` | `AI-CONFIG.md` wirkt laufend, nicht nur beim Anlegen: gleicht sie gegen `.claude/template.json` § `applied_config` ab und setzt Änderungen um — Ergänzungen (Werkzeug/Regelsatz/Wartung/Optimierung nachladen, Schalter setzen) automatisch, Löschen/projektweite Ersetzung (Werkzeug/Regelsatz entfernen, Wartung/Optimierung abschalten, Projektname/Auftraggeber/Orchestrator/Befehls-Schlüssel ändern) nur mit `--yes`; `--check --quiet` läuft zusätzlich automatisch per `SessionStart`-Hook. Meldet/ergänzt zusätzlich (Backlog B37) Tabellen-Schlüssel, die die Template-Fassung von `AI-CONFIG.md` kennt, die ausgefüllte Projektfassung (steht in `keep_local`, wird nie gemergt) aber noch nicht hat — Live-Abgleich per `git show`, nicht über `applied_config`. Lädt setup-lib.py/rename-lib.py/guidelines.py/maintenance-check.py/update-template.py per `importlib` statt deren Logik zu verdoppeln | `python .claude/scripts/sync-config.py --check [--quiet]` (Default) · `--apply [--yes]` · `--adopt` · `--status` | 2026-09-17 |
| `apply-template.py` | Weg 2 „Projekt nachrüsten": kopiert die Agentic-Coding-Grundausstattung aus diesem Template-Checkout in ein bestehendes Repo, ohne dort etwas zu überschreiben; schreibt `.claude/template.json` im Ziel und legt den Remote `template` an. Verschwindet beim Abschluss der Einrichtung (`/act-finalize`) | `python .claude/scripts/apply-template.py --target <ziel-repo> [--dry-run]` (läuft aus dem Template-Checkout heraus) | 2026-09-11 |
| `migrate-project.py` | Weg 2 „Projekt nachrüsten", Struktur-Migration, dünner CLI-Wrapper: erkennt alte KI-Arbeitsordner (z. B. `fable/`, `docs/ki/`) samt Arbeitsdateien und verschiebt sie nach `docs/ai/<template-name>` (`git mv`), meldet Dateien, die im Ziel und im Template existieren und sich unterscheiden (Zusammenführen), meldet vorhandene Regeldateien anderer KI-Werkzeuge (`.clinerules`, `.cursorrules`, …) in einer eigenen Kategorie, erkennt/ersetzt einen projektweiten Orchestrator-Rufnamen. Die eigentliche Logik steckt in `rename-lib.py`; verschwindet beim Abschluss der Einrichtung, die Bibliothek bleibt | `python .claude/scripts/migrate-project.py --plan` (Default) · `--apply` · `--status` · `--rename-orchestrator ALT=NEU` (läuft im Zielrepo) | 2026-09-13 |
| `rename-lib.py` | Bibliothek zu Weg 2 „Projekt nachrüsten", Struktur-Migration: KI-Ordner erkennen/verschieben, Orchestrator-Rufnamen erkennen/ersetzen — von `migrate-project.py` (CLI) und `sync-config.py` (laufender Abgleich) per `importlib` geladen. Bleibt auch nach dem Abschluss der Einrichtung | kein eigener Aufruf — über `migrate-project.py` bzw. `sync-config.py` | 2026-09-13 |
| `maintenance-check.py` | Fälligkeit der wiederkehrenden Wartung (`.claude/maintenance/status.json`) prüfen/pflegen; `--check --quiet` läuft zusätzlich automatisch per `SessionStart`-Hook (meldet nur bei Fälligkeit, Exit immer 0). Optional — existiert nur, wenn `AI-CONFIG.md` § „Wartung" auf „ein" steht | `python .claude/scripts/maintenance-check.py --check [--quiet]` (Default) · `--list` · `--status` · `--done <aufgabe>[,...]\|alle [--date YYYY-MM-DD]` · `--set <aufgabe>=<tage>[,...]` | 2026-09-13 |
| `install-global.py` | `AI-CONFIG.md` § „Globale Ablage": ausgewählte Agenten/Skills und ein kurzer Regelauszug nach `~/.claude/` legen, damit sie in allen Projekten gelten (nur `agents`, `act-commit`+`act-audit-docs`, optional `~/.claude/CLAUDE.md` — die übrigen Skills brauchen Template-eigene Scripte und werden nicht angeboten); Platzhalter werden durch echte Werte aus `.claude/template.json` oder neutrale Begriffe ersetzt; nie ungefragt überschrieben. Verschwindet beim Abschluss der Einrichtung (`/act-finalize`) | `python .claude/scripts/install-global.py --plan` (Default) · `--apply [--force]` · `--status` · `--remove` · `--parts agents,skills,rules` (Default aus `AI-CONFIG.md`) · `--home-dir PFAD`/Env `CLAUDE_GLOBAL_DIR` (Tests) | 2026-09-13 |
| `finish-setup.py` | Skill `/act-finalize`: entfernt nach ausdrücklicher Freigabe alle Werkzeuge, die nur zum Anlegen/Nachrüsten gebraucht wurden (`create-project.py`, `apply-template.py`, `migrate-project.py`, `install-global.py`, die Skills `act-create-project`/`act-apply-template`/`act-finalize`, sich selbst); `--check --quiet` läuft zusätzlich automatisch per `SessionStart`-Hook und erinnert nur, solange der Abschluss noch aussteht | `python .claude/scripts/finish-setup.py --plan` (Default) · `--apply` · `--check [--quiet]` | 2026-09-13 |

| `act-help.py` | Übersicht der Projekt-Befehle aus dem Frontmatter aller `SKILL.md`: Name, Parameter (`argument-hint`), Kurzbeschreibung; mit Namen ein Befehl samt Auslösern. Grundlage für `/act` | `python .claude/scripts/act-help.py [befehl]` | 2026-09-17 |
| `check-refs.py` | Querverweise in der Doku prüfen: zitierte Kürzel (`T<n>`, `Q<n>`, `ADR-<n>`, `S<n>`, `B<n>`) ohne Ziel (tote Verweise) und Ziele ohne Zitat (verwaist). Kennt drei Definitionsformen im Backlog und einen Ignorier-Marker für Formbeschreibungen. Gehört in den Lauf „Doku prüfen und nachziehen" | `python .claude/scripts/check-refs.py` (Default `--check`) · `--root <pfad>` | 2026-09-14 |
| `code-inventory.py` | Bestandsaufnahme eines beliebigen Codebaums — Zeilen je Verzeichnis und Dateityp, größte Dateien, TODO/FIXME-Zählung, Gitignoriertes getrennt ausgewiesen. Für fremde Projekte und das Nachrüsten: Zahlen statt Vermutungen, ohne dass ein Sub-Agent Tausende Dateien liest | `python .claude/scripts/code-inventory.py [--root <pfad>] [--top N] [--json] [--alle]` | 2026-09-14 |
| `guidelines.py` | Regelsätze aus `docs/project/coding_rules.d/` nachladen bzw. entfernen (`AI-CONFIG.md` § `Coding-Guidelines`) | `python .claude/scripts/guidelines.py --list` · `--add <kennung>` · `--remove <kennung>` | 2026-09-13 |
| `feedback.py` | Freiwillige Rückmeldung an den Template-Autor: was sich an der Arbeitsweise bewährt oder gefehlt hat. Sendet **nie** Dateien und **nie** ohne hinterlegte Einwilligung; gesteuert über `AI-CONFIG.md` § `Feedback` (aus/bestaetigen/automatisch/manuell) und `Feedback-Takt` (manuell bis sofort), beides im Script durchgesetzt. Jede Sendung wird nach `docs/ai/template-feedback/` protokolliert — standardmäßig versioniert, auf Wunsch per `.gitignore` lokal (`--protokoll lokal`, wird bei der Einwilligung mitgefragt). Jede Zeichenkette läuft vorher durch eine Prüfung auf Zugangsdaten, Pfade, Mailadressen, IPs und fremde URLs. Was erhoben wird, steuert `Feedback-Umfang` (`a` Kennzahlen aus `git log`/Dateisystem, `b` Regel-/Strukturänderungen als Zahl je Bereich, `c` Werkzeug-Nutzung) — MCP-Server nur als Katalog-Kennung, alles andere nur gezählt. Einträge liegen als einzelne `<thema>.md` mit YAML-Front-Matter unter `docs/ai/template-feedback/` und wandern beim Versand nach `sent/` (Altbestand als Paar `.md` + `.json` wird beim Lesen weiterhin erkannt); `feedback.md` ist der Fragebogen für {{AUFTRAGGEBER}} selbst. **`--direkt` geht immer**, auch bei `Feedback: aus` (dann ohne Projekt-Kennung). Im Template-Checkout selbst verweigert das Script jede Aktion | `python .claude/scripts/feedback.py --status` · `--enable [--repo-url U] [--weg …] [--ausfuellart …] [--protokoll versionieren\|lokal]` · `--disable` · `--add --art … --titel … --text …` · `--direkt "<Text>"` · `--plan` (Default) · `--send [--force] [--yes]` · `--clear` | 2026-09-15 |
| `feedback-check.py` | Erinnert bei Sitzungsstart, wenn eine Rückmeldung fällig wäre — nur bei `Feedback-Takt: adaptiv`. Gemessen wird **Arbeit statt Zeit**: Tage mit Commits seit der letzten Sendung (fällig ab 5), dazu ein Mindestabstand von 48 h. Bewusst **eigenständig** statt im Wartungs-Hook: Die Wartung ist abwählbar, die Rückmeldung hätte sonst keinen Auslöser. Erinnert nur, sendet nie, Exit immer 0 | `python .claude/scripts/feedback-check.py` (SessionStart-Hook) · `--status` | 2026-09-15 |

**Kandidaten (beim nächsten Wartungslauf prüfen):** Dateiübersicht `docs/project/` (Pfad · Zeilen · Datenstand)
für `docs/README.md`, Zählung offener Aufgaben/Fragen, Abhängigkeits-Report-Zusammenfassung.
